# packages

Shared logic/type/UI source of truth.

Rules:
- Update shared behavior here first.
- Keep platform-specific overrides in `apps/*`.
- For Chrome MV3 content scripts, keep inline-compatible outputs until a bundler is introduced.
