# ext-safari

Safari extension platform layer (planned).

Status:
- Runtime wiring: not implemented
- Adapter status: `platform-adapter.js` compatibility shim/stub available
- Feature flags: default OFF

Keep Safari-specific differences in adapter modules only.
